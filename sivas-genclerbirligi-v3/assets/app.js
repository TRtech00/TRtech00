document.addEventListener('DOMContentLoaded',()=>{const modal=document.querySelector('#joinModal');document.querySelectorAll('.join-open').forEach(b=>b.addEventListener('click',()=>modal?.classList.add('open')));document.querySelector('.modal-close')?.addEventListener('click',()=>modal?.classList.remove('open'));modal?.addEventListener('click',e=>{if(e.target===modal)modal.classList.remove('open')});const slider=document.querySelector('[data-slider]');if(slider){let down=false,startX=0,left=0;slider.addEventListener('pointerdown',e=>{down=true;startX=e.clientX;left=slider.scrollLeft;slider.setPointerCapture(e.pointerId)});slider.addEventListener('pointermove',e=>{if(down)slider.scrollLeft=left-(e.clientX-startX)});slider.addEventListener('pointerup',()=>down=false);slider.addEventListener('pointercancel',()=>down=false)}});